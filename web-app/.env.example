# Optional override for the Avalanche Fuji RPC. The default in lib/wagmi.ts
# (https://api.avax-test.network/ext/bc/C/rpc) works fine for the demo, but
# if you hit rate limits during a live judge walkthrough drop a paid RPC here.
NEXT_PUBLIC_FUJI_RPC_URL=
